# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: sqlite3
  database: db/roxanne-lee.db
  #username: rox
  #password: tumadre
  #host: localhost
  #encoding: utf8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    database: roxanne-lee
    username: rox
    password: tumadre
    host: localhost
    encoding: utf8

production:
    adapter: sqlite3
    database: roxanne-lee.db
    username: rox
    password: tumadre
    host: localhost
    encoding: utf8
